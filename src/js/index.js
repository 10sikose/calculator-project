/*const add = (x, y = x) => x + y;
const subtract = (x, y = x) => x - y;
const multiply = (x, y = x) => x * y;
const divide = (x, y = x) => x / y;
const raisePow = (x, y = 2) => x ** y;
const square = x => raisePow(x);
const findRoot = (x, y = 2) => x ** (1/y);
const findSquareRoot = x => findRoot(x);
const negate = x => x > 0 ? -Math.abs(x) : x < 0 ? Math.abs(x) : 0;

console.log(negate(10));
let args = [2];
let operation = add(...args);
console.log(operation);
args = [operation];
args.push(10);
operation = multiply(...args);
console.log(operation);*/

// CONTROL 


